import { MessageDataSource } from './message-data-source';

describe('MessageDataSource', () => {
  it('should create an instance', () => {
    expect(new MessageDataSource()).toBeTruthy();
  });
});
