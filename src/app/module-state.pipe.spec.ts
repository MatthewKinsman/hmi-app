import { ModuleStatePipe } from './module-state.pipe';

describe('ModuleStatePipe', () => {
  it('create an instance', () => {
    const pipe = new ModuleStatePipe();
    expect(pipe).toBeTruthy();
  });
});
