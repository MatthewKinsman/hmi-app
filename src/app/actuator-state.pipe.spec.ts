import { ActuatorStatePipe } from './actuator-state.pipe';

describe('ActuatorStatePipe', () => {
  it('create an instance', () => {
    const pipe = new ActuatorStatePipe();
    expect(pipe).toBeTruthy();
  });
});
